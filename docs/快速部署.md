# 🚀 快速部署到 Railway

## 📝 第一步：准备 GitHub

### 1. 创建 GitHub 仓库
1. 访问 https://github.com/new
2. 仓库名称填：`predict-trading-bot`
3. 选择 Public（推荐）
4. **不要**勾选 "Add a README file"
5. 点击 "Create repository"

### 2. 上传代码

在项目目录打开 **PowerShell**，依次执行：

```powershell
cd C:\Users\Administrator\predict-trading-bot

# 提交代码
git commit -m "Initial commit"

# 添加远程仓库（替换 YOUR_USERNAME 为你的 GitHub 用户名）
git remote add origin https://github.com/YOUR_USERNAME/predict-trading-bot.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

**注意**：
- 如果提示输入用户名和密码，使用：
  - 用户名：你的 GitHub 用户名
  - 密码：**Personal Access Token**（不是登录密码）

### 获取 Personal Access Token（如果需要）：
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token → 勾选 `repo` 权限
3. 复制生成的 token

---

## 🌐 第二步：部署到 Railway

### 1. 注册 Railway
1. 访问 https://railway.app/
2. 点击 "Sign up with GitHub"

### 2. 创建新项目
1. 登录后点击 "New Project"
2. 选择 "Deploy from GitHub repo"
3. 选择 `predict-trading-bot` 仓库

### 3. 配置环境变量

部署后，点击项目 → **Variables** → **New Variable**，依次添加：

| Name | Value |
|------|-------|
| `TELEGRAM_BOT_TOKEN` | `你的_Bot_Token`（从 @BotFather 获取） |
| `TELEGRAM_CHAT_ID` | `你的_Chat_ID`（从 @userinfobot 获取） |
| `PREDICT_API_KEY` | `你的_API_Key`（可选，从 Predict.fun Discord 申请） |
| `MIN_ARBITRAGE_THRESHOLD` | `2.0` |
| `SCAN_INTERVAL` | `10` |

### 4. 重新部署
配置环境变量后，点击 **"Deploy"** 或 **"Restart"**

---

## ✅ 验证部署

1. 在 Railway 点击 **"View Logs"** 查看运行日志
2. 检查你的 Telegram，应该收到测试消息

---

## 💡 日常使用

### 更新代码
```powershell
cd C:\Users\Administrator\predict-trading-bot
git add .
git commit -m "Update code"
git push
```
Railway 会自动重新部署！

### 查看日志
Railway 控制台 → 点击项目 → **View Logs**

### 暂停服务
Railway 控制台 → 点击项目 → **Pause**（暂停不会扣费）

---

## 💰 费用说明

Railway 免费套餐：
- ✅ 每月 $5 免费额度
- ✅ 套利监控不会超出
- ✅ 超出后自动暂停

---

## 🆘 遇到问题？

### 常见错误

**1. Git 推送失败**
```
error: failed to push some refs
```
解决：使用 Personal Access Token 而非密码

**2. Railway 部署失败**
- 检查 Logs 查看错误信息
- 确认所有环境变量已配置

**3. 没有收到 Telegram 消息**
- 确认环境变量正确
- 检查 Railway 日志

---

## 📞 需要帮助？

- Railway 文档：https://docs.railway.app/
- GitHub 文档：https://docs.github.com/
