# 项目结构说明

## 📁 根目录文件

### 核心程序
- **arbitrage_main.py** - 套利监控主程序
- **start_arbitrage.py** - 启动包装器（更好的错误处理）
- **monitor.py** - 本地监控工具
- **health_check.py** - Railway 健康检查
- **main.py** - 自动挂单功能（独立于套利监控）

### 配置文件
- **config.yaml** - 实际配置（包含敏感信息，不提交到 Git）
- **config.yaml.example** - 配置模板（安全，可提交）
- **.env.example** - 环境变量模板
- **requirements.txt** - Python 依赖包
- **nixpacks.toml** - Railway 构建配置

### 文档
- **README.md** - 项目主文档
- **会话总结.md** - 内部开发记录（不提交到 Git）

### Git
- **.gitignore** - Git 忽略规则

## 📂 src/ 目录

### API 客户端
- **api_client.py** - Predict.fun API 客户端
  - `PredictAPIClient` - 真实 API 客户端
  - `MockAPIClient` - 模拟 API 客户端
  - `create_api_client()` - 工厂函数

- **polymarket_api.py** - Polymarket API 客户端
  - `RealPolymarketClient` - 真实 API 客户端
  - `MockPolymarketClient` - 模拟 API 客户端
  - `create_polymarket_client()` - 工厂函数

### 核心功能
- **arbitrage_monitor.py** - 套利监控逻辑
  - `ArbitrageMonitor` - 主监控类
  - `check_arbitrage()` - 检查套利机会
  - `check_reverse_arbitrage()` - 检查反向套利
  - `scan_all_markets()` - 扫描所有市场

- **notifier.py** - Telegram 通知
  - `TelegramNotifier` - Telegram 通知器
  - `send_arbitrage_alert()` - 发送套利警报
  - `send_test_message()` - 发送测试消息

### 辅助功能
- **config_helper.py** - 配置辅助（支持环境变量）
- **strategy.py** - 挂单策略（用于自动挂单）
- **risk_manager.py** - 风险管理（用于自动挂单）
- **order_manager.py** - 订单管理（用于自动挂单）

## 📂 docs/ 目录

- **API申请指南.md** - 如何申请 Polymarket 和 Predict.fun API 访问
- **RAILWAY_DEPLOY.md** - Railway 部署详细指南
- **快速部署.md** - 快速部署到 Railway 的步骤

## 🔧 本地脚本（不提交到 Git）

- **start_monitoring.bat** - Windows 后台启动脚本
- **start_monitoring.vbs** - Windows 隐藏窗口启动脚本

## 📊 数据流

```
┌─────────────────────────────────────────────────────┐
│                   arbitrage_main.py                  │
│                    (主程序入口)                       │
└─────────────────┬───────────────────────────────────┘
                  │
         ┌────────┴────────┐
         │                 │
    ┌────▼────┐      ┌────▼─────┐
    │ Polymarket API   │ Predict.fun API
    │ (真实/模拟)       │ (真实/模拟)
    └────┬────┘      └────┬─────┘
         │                 │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │ ArbitrageMonitor│
         │   (套利监控)     │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │ TelegramNotifier│
         │   (推送通知)     │
         └─────────────────┘
```

## 🚀 运行模式

### 1. 混合模式（默认）
- Polymarket: 真实 API
- Predict.fun: 模拟数据
- 无需 API Key，立即可用

### 2. 真实模式
- Polymarket: 真实 API
- Predict.fun: 真实 API
- 需要两个平台的 API Key

### 3. 模拟模式
- Polymarket: 模拟数据
- Predict.fun: 模拟数据
- 用于开发测试

## 📝 环境变量

| 变量 | 必需 | 默认值 | 说明 |
|------|------|--------|------|
| TELEGRAM_BOT_TOKEN | ✅ | - | Telegram Bot Token |
| TELEGRAM_CHAT_ID | ✅ | - | Telegram Chat ID |
| USE_HYBRID_MODE | - | true | 混合模式 |
| USE_REAL_API | - | false | 真实模式 |
| PREDICT_API_KEY | - | - | Predict.fun API Key |
| MIN_ARBITRAGE_THRESHOLD | - | 2.0 | 最小套利阈值 |
| SCAN_INTERVAL | - | 10 | 扫描间隔（秒） |
| COOLDOWN_MINUTES | - | 5 | 冷却时间（分钟） |

## 🔐 安全注意事项

- **config.yaml** 包含敏感信息，已在 .gitignore 中
- 使用 **config.yaml.example** 作为模板
- 环境变量优先于配置文件
- Railway 环境变量在平台设置，不要硬编码

## 📚 扩展开发

### 添加新的套利策略
在 `src/arbitrage_monitor.py` 中添加新的检查方法

### 添加新的通知渠道
在 `src/notifier.py` 中添加新的通知类

### 添加新的市场映射
在 `src/arbitrage_monitor.py` 的 `_load_market_map()` 中添加

---

**更新日期**: 2026-02-05
