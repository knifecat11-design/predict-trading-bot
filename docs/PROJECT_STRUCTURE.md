# 项目结构说明

## 📁 根目录文件

### 核心程序
- **arbitrage_main.py** - 套利监控主程序（唯一入口）
- **health_check.py** - Railway 健康检查

### 配置文件
- **config.yaml** - 实际配置（包含敏感信息，不提交到 Git）
- **config.yaml.example** - 配置模板（安全，可提交）
- **requirements.txt** - Python 依赖包
- **nixpacks.toml** - Railway 构建配置

## 📂 src/ 目录

### API 客户端
- **api_client.py** - Predict.fun API 客户端
  - `PredictAPIClient` - 真实 API 客户端（支持分页）
  - `MockAPIClient` - 模拟 API 客户端（95个市场）
  - `create_api_client()` - 工厂函数

- **polymarket_api.py** - Polymarket API 客户端
  - `RealPolymarketClient` - 真实 API 客户端（500个市场）
  - `MockPolymarketClient` - 模拟 API 客户端
  - `create_polymarket_client()` - 工厂函数

### 核心功能
- **arbitrage_monitor.py** - 套利监控逻辑
  - `ArbitrageMonitor` - 主监控类
  - `check_arbitrage()` - 检查套利机会
  - `scan_all_markets()` - 扫描所有市场

- **market_matcher.py** - 跨平台市场匹配
  - `MarketMatcher` - 市场匹配器
  - `build_market_map()` - 构建市场映射
  - `_find_best_match()` - 查找最佳匹配

- **notifier.py** - Telegram 通知
  - `TelegramNotifier` - Telegram 通知器
  - `send_arbitrage_alert()` - 发送套利警报

### 辅助功能
- **config_helper.py** - 配置辅助（支持环境变量）

## 📂 docs/ 目录

- **API申请指南.md** - 如何申请 API 访问
- **RAILWAY_DEPLOY.md** - Railway 部署指南
- **快速部署.md** - 快速部署步骤

## 📊 数据流

```
┌─────────────────────────────────────────────────────┐
│                   arbitrage_main.py                  │
│                    (主程序入口)                       │
└─────────────────┬───────────────────────────────────┘
                  │
         ┌────────┴────────┐
         │                 │
    ┌────▼────┐      ┌────▼─────┐
    │ Polymarket API   │ Predict.fun API
    │ (500 市场)       │ (95 模拟/125 真实)
    └────┬────┘      └────┬─────┘
         │                 │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │ MarketMatcher   │
         │  (市场匹配)      │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │ ArbitrageMonitor│
         │  (套利检测)      │
         └────────┬────────┘
                  │
         ┌────────▼────────┐
         │ TelegramNotifier│
         │  (推送通知)      │
         └─────────────────┘
```

## 🚀 运行模式

### 1. 混合模式（默认）
- Polymarket: 真实 API (500市场)
- Predict.fun: 模拟数据 (95市场)
- 无需 API Key，立即可用

### 2. 真实模式
- Polymarket: 真实 API (500市场)
- Predict.fun: 真实 API (125市场，仅历史数据)
- 需要 Predict.fun API Key

## 📝 环境变量

| 变量 | 必需 | 默认值 | 说明 |
|------|------|--------|------|
| TELEGRAM_BOT_TOKEN | ✅ | - | Telegram Bot Token |
| TELEGRAM_CHAT_ID | ✅ | - | Telegram Chat ID |
| USE_HYBRID_MODE | - | true | 混合模式 |
| USE_REAL_API | - | false | 真实模式 |
| MIN_ARBITRAGE_THRESHOLD | - | 2.0 | 最小套利阈值(%) |
| SCAN_INTERVAL | - | 10 | 扫描间隔(秒) |
| COOLDOWN_MINUTES | - | 5 | 冷却时间(分钟) |

## 🔐 安全注意事项

- **config.yaml** 包含敏感信息，已在 .gitignore 中
- 使用 **config.yaml.example** 作为模板
- 环境变量优先于配置文件
- Railway 环境变量在平台设置

## 📈 当前状态

- Polymarket: ✅ 500 个市场（真实 API）
- Predict.fun: ⚠️ 95个模拟市场（真实API仅有历史数据）
- 套利监控: Polymarket ↔ Predict.fun
- Probable.markets: ❌ 已移除（403错误）

---

**更新日期**: 2026-02-06
